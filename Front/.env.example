# URL del backend (carpeta Back). En producción, la URL de Cloud Run.
NEXT_PUBLIC_API_URL="http://localhost:3001"

# URL pública del sitio, usada para OpenGraph y el sitemap.
NEXT_PUBLIC_SITE_URL="https://abautomotores.com.ar"
# Debe coincidir con el REVALIDATE_SECRET del backend
REVALIDATE_SECRET="generar-con-openssl-rand-hex-32"
