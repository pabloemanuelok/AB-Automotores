node_modules
dist

# Variables de entorno: nunca al repo
.env
.env.*
!.env.example

# Cliente generado por Prisma (se regenera en cada build)
/generated

# Documentación que instala `prisma init` en el proyecto
/.agents
/.claude
/.windsurf
skills-lock.json
